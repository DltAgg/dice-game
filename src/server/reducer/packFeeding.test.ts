import { describe, it } from "vitest";

/** Parked: creature↔creature attribute tokens removed (spec 016 Phase 6). */
describe.skip("Share the Kill", () => {
  it("parked — pack feed retarget", () => {});
});

describe.skip("Den Share", () => {
  it("parked — pack feed retarget", () => {});
});

describe.skip("Pack Share", () => {
  it("parked — pack feed retarget", () => {});
});
