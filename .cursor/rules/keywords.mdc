---
description: Use docs/KEYWORDS.md for print, tokens, hooks, and new mechanics
alwaysApply: true
---

# Keywords

Print vocabulary lives in [`docs/KEYWORDS.md`](docs/KEYWORDS.md). The **Rules**
tab shows its player sections (HTML comments are agent-only).

When authoring cards, new mechanics, hooks, tokens, keywords, or other rules
print:

1. Read `docs/KEYWORDS.md` first.
2. Prefer an existing operator + argument over a new verb — `[Mark N Toxin]`,
   `[Mark N Toxin on attacks]`, `[Forge N]` on their die, `[Overcharge]`
   (spec `021` grammar / master rule — do **not** reprint on every
   card), `[Unlock: Mechanical]` on creature attacks (spec `028` — showing
   faces, **not** pile `[Requires]` / `[Spend]`). Do **not** mint Dose /
   Envenom / Brand / Contaminate for a slightly different window. Spec `013`
   `optional-overcharge` is a Mechanical face-marker opcode, not this keyword.
   Do **not** mint `[Reforge]` / `[Cross forge]` (retired overwrite-without-draw).
   `[Desynthesize]` is not a forge and not `[Stamp]`.
   Do **not** print `[Generate]` on tactics, creatures, rituals, equipment, or
   overloads — extra pips are faces (`pips` / dual-pip line) and `[Overcharge]`.
3. A new **token** (creature counter, Shield, face marker) gets a rulebook
   paragraph and joins **X** on `[Mark]` / `[Strip]`. It does not get its own
   keyword. Attribute exclusives still gate the **argument**, not the verb.
4. Add a **physics** keyword (`[Empower]`, `[Pierce]`, `[Reduce]`, `[Prevent]`) only when
   the operation is not placing or removing a token.
5. Timing prefixes stay prefixes: `On roll: [Mark 1 Toxin].` Never “Whenever…”.
6. Same change: update `docs/KEYWORDS.md`. If play itself changed, also
   `docs/RULEBOOK.md`. Map print to AST opcodes (`mark`, `strip`, `modify`).
   New tokens are `X` arguments, not new opcodes. Unique one-offs stay named
   until a second card needs the same composition.

Do not mass-rewrite existing `rulesText` unless asked. **New and edited**
print uses the glossary.
